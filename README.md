# Server SSH Script

## Overview
This script is used to execute operations on multiple servers via SSH using a provided server list file and a remote user.

## Usage

```bash
script.sh -f <server_list_file> -u <remote_user>
